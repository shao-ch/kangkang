<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kangkang.dao.TbAfterSaleDao">


    <select id="selectListByCid" resultType="com.kangkang.store.entity.TbAfterSale">
        select * from tb_after_sale   WHERE tb_category_id = #{cid}
    </select>
</mapper>